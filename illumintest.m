function [g_acc, k_acc] = illumintest()
    project1(23,98,0,5,1);

% Manually copy/paste results here (no time to make it pretty!!)
%     figure;
%     predicted = discretize([ones(1,102)], [-1 0 1], 'categorical', {'expression', 'neutral'});
%     actual = discretize([-1*ones(1,16) ones(1,86)], [-1 0 1], 'categorical', {'expression', 'neutral'});
%     confusionchart(predicted, actual);
end